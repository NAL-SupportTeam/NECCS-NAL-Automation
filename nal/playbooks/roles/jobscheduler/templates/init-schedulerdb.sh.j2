#!/usr/bin/sh

mysql -h localhost -pi-portal -e "create user 'root'@'{{ nal.ap.nodes[0].name }}' identified by 'i-portal';" 2>/dev/null
mysql -h localhost -pi-portal -e "set password for 'root'@'{{ nal.ap.nodes[0].name }}'=password('i-portal');" 2>/dev/null

mysql -h localhost -pi-portal -e "create user 'root'@'{{ nal.ap.nodes[1].name }}' identified by 'i-portal';" 2>/dev/null
mysql -h localhost -pi-portal -e "set password for 'root'@'{{ nal.ap.nodes[1].name }}'=password('i-portal');" 2>/dev/null

mysql -h localhost -pi-portal -e "GRANT ALL PRIVILEGES ON scheduler.* TO root@\"%\" IDENTIFIED BY 'i-portal' WITH GRANT OPTION;" 2>/dev/null

